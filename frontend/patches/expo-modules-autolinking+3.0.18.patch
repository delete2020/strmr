diff --git a/node_modules/expo-modules-autolinking/scripts/ios/cocoapods/installer.rb b/node_modules/expo-modules-autolinking/scripts/ios/cocoapods/installer.rb
index 1e9b4c7..451bfc0 100644
--- a/node_modules/expo-modules-autolinking/scripts/ios/cocoapods/installer.rb
+++ b/node_modules/expo-modules-autolinking/scripts/ios/cocoapods/installer.rb
@@ -32,6 +32,12 @@ module Pod
       # Call original implementation first
       _original_run_podfile_post_install_hooks.bind(self).()
 
+      # Skip this workaround when building the dedicated tvOS target.
+      if ENV['EXPO_TV'] == '1'
+        Pod::UI.puts "[Expo] ".yellow + "Skipping Codegen workaround for tvOS build"
+        return
+      end
+
       # Next we'll perform an Expo workaround for Codegen in React Native where it uses the wrong output path for
       # the generated files. This can be remove when the following PR is merged and released upstream:
       # https://github.com/facebook/react-native/pull/54066
